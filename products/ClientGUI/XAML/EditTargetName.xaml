<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Target Name"
        VerticalAlignment="Top" Width="450" MinWidth="450" Height="300" MinHeight="300" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" >
    <Grid Name="MainGrid" Margin="0,0,0,0">
        <StackPanel Margin="5,5,5,5">
            <RadioButton Name="IQNFormatRadio" Content="iqn. format" Margin="0,2,0,2"/>
            <StackPanel Margin="10,0,0,0">
                <Grid Margin="0,2,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*" />
                        <ColumnDefinition Width="6*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="YearTextBlock" Text="Year(YYYY)" Padding="0,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBox Grid.Column="1" Name="YearTextBox" Text="0" Padding="2,2,2,2" />
                </Grid>
                <Grid Margin="0,2,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*" />
                        <ColumnDefinition Width="6*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="MonthTextBlock" Text="Month(MM)" Padding="0,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBox Grid.Column="1" Name="MonthTextBox" Text="0" Padding="2,2,2,2" />
                </Grid>
                <Grid Margin="0,2,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*" />
                        <ColumnDefinition Width="6*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="DomainTextBlock" Text="Reversed domain name" Padding="0,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBox Grid.Column="1" Name="DomainTextBox" Text="0" Padding="2,2,2,2" />
                </Grid>
                <Grid Margin="0,2,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*" />
                        <ColumnDefinition Width="6*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="OptStrTextBlock" Text="Option string" Padding="0,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBox Grid.Column="1" Name="OptStrTextBox" Text="0" Padding="2,2,2,2" />
                </Grid>
            </StackPanel>
            <RadioButton Name="EUIFormatRadio" Content="eui. format" Margin="0,2,0,2"/>
            <StackPanel Margin="10,0,0,0">
                <Grid Margin="0,2,0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4*" />
                        <ColumnDefinition Width="6*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Name="EUI64TextBlock" Text="EUI-64 Identifier(16 hex digits)" Padding="0,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                    <TextBox Grid.Column="1" Name="EUI64TextBox" Text="0" Padding="2,2,2,2" />
                </Grid>
            </StackPanel>
            <RadioButton Name="UnknownFormatRadio" Content="Other" Margin="0,2,0,2"/>
            <TextBox Name="OtherTextBox" Text="0" Padding="2,2,2,2" Margin="10,2,0,2" />
            <Grid Margin="0,15,0,0" >
                <Button Name="RandomButton" Content="Random" Margin="0,0,0,0" TabIndex="3" HorizontalAlignment="Left" Width="83"/>
                <Button Name="OKButton" Content="OK" Margin="0,0,93,0" TabIndex="3" HorizontalAlignment="Right" Width="83"/>
                <Button x:Name="CancelButton" Content="Cancel" TabIndex="4" HorizontalAlignment="Right" Width="83"/>
            </Grid>
        </StackPanel>
    </Grid>

    
</Window>
    